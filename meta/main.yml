galaxy_info:
  author: haxwithaxe
  description: Setup a glusterfs cluster
  license: GPL-3.0
  min_ansible_version: 2.1

  platforms:
  - name: Debian
    versions:
      - 12
   - name: Ubuntu
     versions:
     - 22.04

  galaxy_tags:
    - glusterfs
    - filesystem
    - tls

dependencies:
  - geerlingguy.glusterfs
